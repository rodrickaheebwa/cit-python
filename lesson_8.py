# sets

# all elements of a set must be immutable (so no lists as elements)
# a set can't be indexed because it's unordered

# you can eliminate duplicates from a list by converting it into a set

# functions